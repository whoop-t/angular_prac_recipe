export class Ingredient {
  //With TypeScript, you can declare a class as below and it will,
  //automatically set the values correctly when they are passed in
  constructor(public name: string, public amount: number) {}
}
